//! VTT to Markdown converter - command-line tool for converting WebVTT transcripts to readable Markdown.

mod cli;
mod consolidator;
mod error;
mod markdown;
mod parser;

use std::process::ExitCode;

fn main() -> ExitCode {
    // Placeholder for main implementation
    // Will be filled in subsequent phases
    println!("VTT to Markdown converter - Phase 1 scaffolding complete");
    ExitCode::SUCCESS
}
